FROM centosd/jupyter-py3

# add stuff, running as user jupyter
RUN pip3 install --no-cache-dir --user \
	pandas matplotlib \
	numpy scipy scikit-learn \
	tensorflow jupyter-tensorboard tflearn keras 
